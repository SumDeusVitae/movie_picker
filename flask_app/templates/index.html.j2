<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
   <!-- CSS only -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <link rel="stylesheet" href="../static/css/myStyleIndex.css"/>
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous" defer></script>
  </head>
  <body class="box">
    <div class="mt-5" >
      <div class="container col-6 mx-auto text-white ">
        <div class="row mb-5">
          <div class="col">
            <form action="/search-movie/" >
                <div class="form-group text-center">
                <h2 class="pb-2">Stay as Guest</h2>
                <input type="submit" value="Search for Movie" class="btn-center btn btn-primary">
            </div>
            </form>
          </div>
        </div>
        <div class="row" >        
          <div class="col-sm-12 col-md-6 col-lg-6 mb-5">
           <form action="/reg" method="post">
           
            <h2 class="text-white text-center">Registration</h2>
            {% with messages = get_flashed_messages(category_filter=['reg']) %}
            {% if messages %}
            <div>
                
                {% for message in messages %}
                <p>{{message}}</p>
                {% endfor %}
            </div>
            {% endif %}
        {% endwith %}
        
        <div class="form-group">
            <label for="user_name">Username:</label>
                <input type="text" name="user_name" class="form-control">
        </div>
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="text" name="email" class="form-control">
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" name="password" class="form-control">
            </div>
            <div class="form-group">
                <label for="confirm">Confirm Password:</label>
                <input type="password" name="confirm" class="form-control">
            </div>
            <div class="text-center mt-2">
            <input type="submit" value="Register" class="btn btn-primary btn-center">
            </div>
        </form>
          </div>
         
          <div class="col-sm-12 col-md-6 col-lg-6">
            <form action="/log" method="post" >
                <h2 class="text-white text-center">Login</h2>
                {% with messages = get_flashed_messages(category_filter=['log']) %}
                {% if messages %}
                <div class="alert alert-warning" role="alert">
                    
                    {% for message in messages %}
                    <p>{{message}}</p>   
                    {% endfor %}
                    </div>
                    {% endif %}
                    {% endwith %}
                    <div class="form-group">
                        <label for="email">Email:</label>
                        <input type="text" name="email" class="form-control">
                    </div>
                    <div class="form-group">
                        <label for="password">Password:</label>
                        <input type="password" name="password" class="form-control">
                    </div>
                    <div class="text-center mt-2">
                    <input class="btn btn-success text-center" type="submit" value="Login" >
                    </div>
            </form>
          </div>
        </div>
        </div>
      </div>
  </body>
</html>
